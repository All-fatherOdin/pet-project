export enum Theme {
  LIGHT = "light",
  DARK = "dark"
}

export enum Color {
  PRIMARY = "primary",
  SECONDARY = "secondary",
}

export const LOCAL_STORAGE_THEME_KEY = "theme"

export const DEFAULT_THEME = Theme.LIGHT